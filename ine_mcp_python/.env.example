# Redis Configuration (optional - falls back to in-memory if not available)
REDIS_URL=redis://localhost:6379

# Index directory (where FAISS index and metadata are stored)
INDEX_DIR=data

# Logging level
LOG_LEVEL=INFO
